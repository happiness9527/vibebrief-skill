# Session Brief Example

## Input

Fictional project: DemoDesk.

```text
I fixed the workspace window sync issue by changing demo_web_app.py and controller.py.
The app should now open the correct course window when /workspace starts.
```

## Possible VibeBrief Output

This looks like a workspace startup fix.

The AI says it changed the web entry file and the controller, so the work probably affects how the workspace opens. The result is still unconfirmed because no run output, screenshot, or test result was shown.

Next step: ask the AI to start `/workspace` and show the result before adding new features.
