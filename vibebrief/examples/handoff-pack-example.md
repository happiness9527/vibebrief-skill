# Handoff Pack Example

## Input

Fictional project: CoursePilot.

```text
I need to start a new chat. We built the course outline generator,
fixed the preview refresh bug, but export is not working yet.
```

## Possible VibeBrief Output

CoursePilot helps creators generate and preview course outlines.

Current state: outline generation exists; preview refresh was reportedly fixed; export is still unresolved.

Next AI should first confirm preview behavior, then debug one export path. Avoid adding new export formats until one path works.
